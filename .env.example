# Infrastructure config — read automatically by docker-compose.
# This file is gitignored; .env.example is the committed template.
POSTGRES_USER=sales_user
POSTGRES_PASSWORD=sales_pass
POSTGRES_DB=sales_db
POSTGRES_PORT=5434
REDIS_PORT=6379
